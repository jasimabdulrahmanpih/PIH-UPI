'use client';
import {useState} from 'react';
import {ArrowLeft,Smartphone,Tablet} from 'lucide-react';
import {Tabs,TabsList,TabsTrigger} from '@/components/ui/tabs';
const sizes={compact:{width:360,height:780},phone:{width:390,height:844},tablet:{width:768,height:1024}};
export default function MobilePreview(){const [size,setSize]=useState<keyof typeof sizes>('phone');const viewport=sizes[size];return <main className="device-preview"><header className="device-header"><a href="/"><ArrowLeft size={17}/>Back to workspace</a><span>PIH / RESPONSIVE PREVIEW</span></header><section className="device-intro"><h1>The same workspace.<br/><em>Made for the moment.</em></h1><p>Use the live app below. Switch views, open a request, or make a demo decision.</p><Tabs value={size} onValueChange={v=>setSize(v as keyof typeof sizes)} aria-label="Preview size"><TabsList><TabsTrigger value="compact"><Smartphone size={15}/>Small phone</TabsTrigger><TabsTrigger value="phone"><Smartphone size={15}/>Phone</TabsTrigger><TabsTrigger value="tablet"><Tablet size={15}/>Tablet</TabsTrigger></TabsList></Tabs><span className="viewport-caption">{viewport.width} × {viewport.height} CSS pixels · Interactive web preview</span></section><div className="device-stage"><div className="device-shell" style={{width:viewport.width+16}}><iframe title="PIH mobile responsive workspace" src="/?device-preview=1" style={{width:viewport.width,height:viewport.height}}/></div></div><p className="device-footnote">The frame uses the actual responsive app. Sample decisions are local to this preview session.</p></main>}
